# Z0 · 审问一台世界模型

路径：

```text
notebooks/07_evaluation/Z0-test-a-world-model.ipynb
```

Z0 不增加新网络，只练六项跨路线能力：

1. 多步 horizon 曲线；
2. 固定起点的反事实动作；
3. 单变量 OOD；
4. 不确定性校准；
5. Planner 漏洞；
6. run manifest 与硬件证据。

Notebook 使用解析 toy，可在 CPU 直接运行。完成后把同一套检查迁移到自己的 PA1 模型。
